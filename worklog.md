# Worklog

- Fixed revenue reporting to recognize only posted/sent/paid invoices and aggregate the materialized invoice total instead of mutable order/product data. Added focused tests for status filtering, invoice-level adjustments, deleted-live-data independence, and quarterly/customer/annual grouping.
- Replaced duplicated server/client response interfaces with a shared Zod contract package whose TypeScript models are inferred from runtime schemas. Server DTO mappers and report aggregators now validate their output, while Prisma remains the generated source for internal database record types.
- Added a pure bigint-backed pricing engine for product, hourly, subscription, tiered, discount, tax, and aggregate calculations, returning fixed two-decimal money strings. Unit tests cover float-error regressions, rounding/tier boundaries, floors/ceilings, invalid inputs, and exact aggregation; controller cutover will follow the database expansion.
- Replaced the hand-written rate-tier type guards with the shared Zod schema and inferred type, so persisted pricing JSON and API models use one executable definition. Added regression tests for valid/null storage and malformed, non-finite, or invalid-bound tiers.
- Added an exact legacy baseline plus an expand-only financial-integrity migration: nullable Decimal twins, order-time product/rate/pricing snapshots, invoice bill-to/line snapshots, constrained lifecycle transitions, and database-enforced posted invoice/order immutability. The migration preserves legacy columns, performs no table scan or destructive rebuild, and includes a tested baseline/deploy/batched-backfill runbook with explicit SQLite and PostgreSQL guidance.
- Added strict Zod request contracts and inferred types for every existing API route, covering path/query/body shape, ISO dates, positive quantities and payments, unique IDs, tier consistency, and transmission enums. Routers now validate before invoking business logic and return stable field-level `400 VALIDATION_ERROR` responses; focused contract tests exercise valid and invalid payloads.
- Hardened invoice transmission so only posted/sent invoices can be delivered, snapshotted recipients are revalidated with Zod, PDFs are rendered and attached before success is recorded, and missing portal/API configuration no longer falls back to `"unknown"`. Successful transmission plus invoice state now commit atomically, while rendering/provider/attachment failures create redacted `FAILED` records; behavioral tests verify ordering and negative paths.
- Added `plan.md` as the ordered implementation ledger and `whatsnext.md` for explicitly deferred delivery outbox, provider callback, customer webhook, and reminder work. The plan records completed commits, engineering constraints, and the remaining financial-integrity/test/API hardening sequence for subagent handoffs.
- Declared Zod as a direct server dependency because request middleware imports it at runtime/typecheck time, rather than relying on workspace dependency hoisting through the contracts package.
- Added the canonical Decimal persistence boundary for fixed-scale money, quantity, and percentage strings, Decimal-first legacy fallback, safe temporary float dual-writes, and exact addition/comparison. Behavioral tests cover binary-float regressions, precision/scale overflow, Decimal-like values, fallback precedence, percentage limits, and round-trip safety.
- Added a separate expand-only currency migration with nullable ISO-code fields for products, rates, orders, invoices, and payments; it performs no backfill or table rebuild. SQLite guards enforce uppercase codes, finalized order/invoice/payment immutability, and order/invoice/payment-application currency consistency, while the runbook defines dependency-ordered bounded backfill and reconciliation.
- Extended the shared Zod response contract with canonical exact money/quantity/percentage strings, currency codes, exact tier schedules, and additive Decimal-derived totals/balances across billing resources. Contract tests prove legacy numeric responses remain accepted during rollout while malformed scale, precision, percentage, and currency values are rejected.
- Wired product, rate, discount, order, invoice, and payment response mappers to prefer Decimal fields and captured snapshots, emit canonical exact-string amounts/totals/balances, and retain legacy numeric fields for compatibility. Behavioral mapper tests verify Decimal-first precedence, captured product/customer identity, exact aggregation/subtraction, and detection of overapplied negative balances.
- Added a pure Zod-backed payment-allocation planner that enforces positive canonical amounts, unique targets, customer/currency ownership, eligible invoice states, and both payment and invoice remaining balances. Behavioral tests cover partial/exact allocation, every rejection path, deterministic plans, and a stale-snapshot interleaving that proves persistence still needs compare-and-set or serializable transactions.
- Changed rate and combo-discount writes to persist exact Decimal values alongside compatible legacy numerics, and changed tier JSON to store canonical decimal strings while still reading existing numeric rows. Behavioral tests cover exact/legacy agreement, mixed-version reads, precision rejection, and invalid percentage bounds.
- Added an authoritative exact order-line amount through an expand-only nullable migration, because tiered totals are not always reproducible from a four-decimal blended unit price. Existing rows retain a compatibility fallback until the bounded snapshot backfill, and the migration performs no scan or rewrite.
- Replaced payment application writes with a serializable, retryable ledger workflow that derives balances from immutable applications, reconciles stored totals, enforces ownership/currency/status/remaining-balance invariants, and atomically compare-and-sets invoice balances. Behavioral tests cover exact dual-writes, rollback on failed persistence, rejection before writes, and two stale concurrent allocations where only one may commit.
- Extracted a reusable Express app factory and reduced the production entrypoint to listener startup. Real HTTP-boundary tests verify stable Problem Details for unknown routes and malformed JSON plus redaction of unexpected internal errors; the app also disables the framework fingerprint header.
- Added a Zod-backed versioned order-pricing snapshot calculator that captures product/rate/tier/discount/currency inputs and produces canonical exact quantities, prices, and authoritative amounts with an explicit rounding policy. Tests cover binary-float regressions, deterministic compounded discounts, tier floors, source mutation after capture, currency mismatch, uncovered tiers, duplicate discounts, and zero quantities.
- Cut order reads, quantity edits, draft invoice synchronization, invoice creation, and idempotent posting over to immutable order-time snapshots and exact Decimal dual-writes; live product/rate/discount edits can no longer rewrite history. The workflow now uses transactions, collision-safe UUID-derived references, ownership/finalization guards, and CI runs a production-migration-backed temporary-database scenario covering live-price mutation, draft resync, repeat posting, and finalized-order rejection.
- Replaced the fixed 64 KiB invoice buffer with a deterministic, structurally valid, multi-page PDF writer that prefers exact snapshotted Decimal amounts and escapes document text safely. Tests validate PDF cross-reference integrity and determinism, exact-value rendering, corrupt-Decimal rejection, and successful rendering of 5,000 lines beyond the former buffer limit.
- Added a stable RFC 9457-style Problem Details boundary for not-found, uniqueness/concurrency conflicts, preconditions, domain invariants, malformed JSON, and redacted unexpected failures while preserving the existing validation-error body for client compatibility. Real HTTP tests cover async Prisma-shaped and typed errors, secret redaction, malformed input, unknown routes, and injectable private error logging.
- Added an expand-only explicit accounting-date and singleton closed-through control with monotonic close, backfill-before-close, closed-period posting/redating, and finalized-date immutability guards at both the database and controller boundaries. Revenue reports now group by UTC-stable accounting dates and aggregate Decimal totals exactly, exposing additive exact totals; tests cover timezone/month/leap boundaries, closed-period behavior, exact `0.1 + 0.2`, and a fresh five-migration database.
- Added `/api/v1` as a non-breaking alias while preserving every existing `/api` route and advertising the registered `successor-version` link on legacy responses without inventing a sunset date. Added PATCH aliases for the existing partial order and invoice updates while retaining PUT compatibility, with real HTTP tests proving legacy and versioned validation contracts match.
- Added constant-time bearer API-key authentication for both legacy and v1 routes when configured, and made production startup fail closed when its secret is absent. Real HTTP tests cover missing, incorrect, and accepted credentials; this is a deployable perimeter control, while per-user authorization and tenant isolation remain separate follow-up work.
- Added a Zod-backed, repository-injected financial backfill engine with bounded primary-key batches, transaction-coupled checkpoints, idempotent write contracts, dry-run scanning, injected throttling, exact Decimal reconciliation gates, and typed unsafe-history stops. Behavioral tests verify restart after an interrupted batch without skipped rows, no-write dry runs, deterministic exact totals, mismatch cutoff, unsafe historical refusal, and unstable-source-order rejection.
- Added the first concrete invoice Decimal/accounting-date backfill with an expand-only checkpoint migration, safe default dry-run CLI, exact line/application reconciliation, and refusal to fabricate unexplained adjustments or round unrepresentable legacy floats. A fresh migrated-database test proves preview isolation, transactional progress, exact persisted values, and restart idempotence.
- Rebuilt the development seed around the production pricing-snapshot and Decimal boundaries, producing fully reconciled orders, invoices, payments, currencies, and accounting dates instead of legacy-only floats. Fresh-database verification found no missing exact fields or ledger mismatches and proved live catalog/rate mutation leaves stored financial history unchanged; the seed now refuses to erase finalized history and directs disposable environments through the explicit reset path.
- Added exact decimal-string request contracts for money, quantities, tier bounds, and percentages while retaining legacy numeric inputs for existing API clients. Browser write paths now send their untouched decimal text instead of coercing through JavaScript `Number`; boundary tests cover binary-float-sensitive ordering, scale/precision limits, ambiguous formats, positive-value rules, and exact dual writes.
- Added durable, database-backed `Idempotency-Key` handling for mutating legacy and v1 API operations, including stable client/operation scoping, request fingerprint conflicts, exact completed-response replay, and fail-closed concurrent or interrupted reservations. The expand-only migration touches no business rows, and real migrated-database tests prove persistence and concurrent requests cannot double-apply.
- Added a pinned PostgreSQL 16 CI contract lane alongside the SQLite demo runtime to exercise native `NUMERIC(19,4)`, enum/currency/snapshot constraints, atomic payment failure, row-lock contention, and serializable stale-writer aborts in independent database sessions. The isolated bootstrap is explicitly non-production; its README requires replacement with reviewed additive PostgreSQL migrations before that datasource is supported.
- Made versioned migrations the only supported setup/reset path, removing `prisma db push` so local databases cannot silently omit custom integrity triggers. A fresh-database smoke test verifies every migration, required trigger installation, and direct-SQL status enforcement; a disposable reset produced all seven migrations, 47 triggers, reconciled seed data, and clean integrity checks.
- Made the implemented currency policy explicit and enforceable: shared Zod contracts, SQLite triggers, and the PostgreSQL contract now accept USD only until other currencies have defined minor-unit, rounding, display, and settlement semantics. The expand-only migration leaves historical rollout columns nullable, performs no scan, and adds a direct-SQL rejection test for unsupported currencies.
- Switched browser rendering, summaries, and editable defaults to prefer exact Decimal response fields, with bigint-backed summation, positivity checks, and currency formatting instead of binary-float billing arithmetic. Legacy numeric response fields remain display fallbacks during rollout, while tests cover `0.1 + 0.2`, values beyond JavaScript's safe integer range, exact zero, and cent rounding.
- Added expand-safe database guards that freeze captured order provenance and commercial identity, make payment receipts immutable and non-deletable, and make payment applications append-only. Fresh-migration direct-SQL tests prove prohibited repointing, mutation, and deletion fail while the deliberate one-time null-to-exact/currency backfill path remains available.
- Added a dry-run-first, throttled, five-stage legacy financial backfill for product/rate/tier/discount and payment/application exact fields, with independent transactional checkpoints and idempotent restarts. It preserves agreeing exact data and stops on unrepresentable floats, malformed tiers, unsupported currency, cross-customer/currency applications, overapplication, or ledger mismatches; unknowable historical order terms remain explicitly excluded.
- Standardized expected operational failures on a shared, strict Zod Problem Details contract, mapping order, invoice, delivery, payment, concurrency, and database-constraint conditions to stable 404/409/412/422 responses. Behavioral HTTP and controller tests verify exact public codes and statuses while ensuring database metadata, recipient values, and unexpected failures remain redacted.
- Refreshed `plan.md` into a commit-linked implementation ledger covering the exact-arithmetic, snapshot, ledger, migration, API, and test work already landed. The remaining queue is now ranked by integrity risk: evidence-safe historical cutover and reconciliation, append-only payment reversals, tenant authorization, API/scale work, continuous controls, then operational hardening.
- Added a bounded, read-only financial reconciliation command with strict Zod result and issue-code contracts. It audits exact/currency/snapshot/accounting completeness, snapshot arithmetic, invoice and payment ledgers, ownership/currency, lifecycle consistency, and pagination safety; clean seeded data passes while deliberate corruption and incomplete scans produce stable nonzero outcomes.
- Added an evidence-only, dry-run-first order-pricing snapshot backfill that reconstructs missing exact and captured fields solely from each row's validated persisted snapshot. It never reads live catalog/rate/discount terms, stops on missing or contradictory history with typed Zod-backed codes, derives currency only from agreeing evidence, and is bounded, throttled, checkpointed, and restart-idempotent.
- Added append-only exact payment-application reversals with reason, open accounting date, server-derived actor, immutable database guards, and serializable insert-plus-invoice CAS behavior. The nested REST contract exposes gross/reversed/net amounts, later allocations reuse released payment capacity, and tests cover partial/full correction, delivery-evidence status restoration, closed periods, wrong targets, stale interleavings, and direct database integrity attacks.
- Extended the read-only financial reconciliation gate to validate reversal metadata and exact amounts, subtract valid reversals from invoice/payment application totals, and stop on malformed or cumulative over-reversal history. Behavioral fixtures prove legitimate corrections remain clean while invalid dates and excessive compensating entries produce stable typed violations.
- Added a deterministic architecture-integrity ratchet to `bun run check`: it rejects new Prisma float columns outside the explicit legacy allowlist, raw numeric coercion in financial code, ledger mutations outside the approved path, and hand-written exported API model types. Fixture-driven tests exercise each `ARCH001`–`ARCH004` violation, including multiline bypass attempts, and diagnostics report stable file/line/rule identifiers.
- Added a dry-run-first invoice identity snapshot backfill that fills finalized line SKU/unit only through unique one-to-one matches against immutable order snapshots and refuses to source issued bill-to identity from mutable customers. It stops on missing, ambiguous, conflicting, or invalid evidence; draft-only refresh, live-data independence, finalized database guards, and restart idempotence are covered behaviorally.
- Upgraded Express, tsx, Vite, and the React plugin, pinned audited transitive replacements for `qs` and `deepmerge-ts`, and marked the browser package as ESM for Vite 8's native configuration path. The production client build, full 148-test behavior suite, integrity checks, and live Bun audit now pass with zero reported vulnerabilities.
- Refreshed `plan.md` to record the completed evidence-only snapshot backfills, reversal-aware reconciliation, architecture ratchet, and dependency hardening. The active P0 queue now centers on tenant isolation and deployment-gated contract migrations, with external delivery infrastructure still explicitly deferred to `whatsnext.md`.
- Added an expand-only tenant ownership foundation with enum-backed least-privilege roles, hashed API-key metadata, issuer-scoped external identities, nullable indexed/FK-backed ownership, and per-tenant accounting controls. Fresh-migration tests prove the legacy-null rollout remains compatible while direct SQL cannot create cross-tenant commercial links, reassign established identity/ownership, or delete referenced tenants.
- Added public, redacted liveness and database-readiness contracts plus a bounded graceful shutdown lifecycle for SIGTERM/SIGINT. Deterministic tests prove health/auth isolation, readiness failure redaction, drain-before-disconnect ordering, deadline-driven socket termination, and coalesced repeated shutdown requests.
- Added a dry-run-first, checkpointed legacy-tenant ownership backfill with a stable `legacy-default` identity and relationship reconciliation before every batch. Fresh migrated-database scenarios prove interruption/restart safety, batch rollback on ambiguous ownership, coexistence with already-owned tenant data, tenant-identity collision handling, accounting-close copying, and idempotent reruns.
- Replaced raw Prisma lifecycle/method strings with generated enums for orders, invoices, transmissions, and idempotency records without changing SQLite's existing TEXT representation. Narrower delivery and payment boundaries now consume the shared contract enums, and `ARCH005` prevents these persistence fields from regressing to untyped strings.
- Added a strict, server-derived tenant principal for protected requests, with database API keys resolved by prefix plus versioned HMAC-SHA256 under a strong pepper and the legacy key explicitly confined to `legacy-default`. Idempotency reservations now persist tenant ownership and scope replay by verified tenant and credential; HTTP and migrated-database tests prove revoked/invalid key rejection, client-header distrust, cross-tenant key isolation, and secret-safe failures.
- Added a shared role-authorization boundary with stable 401 versus 403 Problem Details and explicit read, billing-write, and admin role sets. Unit tests ensure missing principals cannot pass and viewer/billing privileges cannot be escalated.
- Scoped customer, product, rate, and combo-discount reads to the authenticated tenant and reserved catalog mutations for administrators; foreign identifiers now behave exactly like missing ones and cannot create partial writes. The canonical seed now provisions `legacy-default` ownership and per-tenant accounting control, while a fresh two-tenant HTTP scenario proves catalog isolation and viewer denial.
- Scoped every order, invoice, delivery, and transmission path to the authenticated tenant, made new commercial records inherit that tenant, and moved invoice close checks to per-tenant controls. A fresh two-tenant HTTP scenario proves foreign reads and create/edit/post/send/refresh attempts return indistinguishable 404s with zero writes, while viewers retain read-only access.
- Corrected the catalog isolation harness to normalize every supported Fetch `HeadersInit` form before adding authentication and content headers, keeping the type-aware lint gate valid without narrowing test behavior.
- Scoped payment receipts, applications, reversals, and revenue reports to the authenticated tenant, made new receipts inherit ownership, and moved reversal close checks to per-tenant controls. Fresh two-tenant database tests prove list/report isolation, cross-tenant ledger attempts roll back without changes, one tenant's close cannot block another, and viewers cannot write; report compatibility numbers now use the reviewed Decimal boundary instead of raw coercion.
- Added bounded request correlation before public and protected routes, rejecting unsafe client IDs while echoing valid or cryptographically generated IDs. Unexpected failures now emit a strict redacted event containing only operational metadata; HTTP tests prove secrets, request bodies, cookies, query values, and raw errors never enter the default log path.
- Added a strict tenant API-key issuance and monotonic revocation lifecycle with cryptographically random opaque credentials, versioned HMAC storage, bounded collision handling, and one-time plaintext output. Fresh-migration tests prove issued keys resolve, revoked keys fail, invalid and collision paths create no partial records, stored rows contain no secret, and CLI failures cannot echo arbitrary error details.
- Added an expand-only optimistic-concurrency foundation with nullable guarded resource versions for mutable aggregate roots and no deploy-time scans, rebuilds, or redundant indexes. Strict strong-ETag and `If-Match` helpers bind versions to one resource, reject weak/wildcard/list/cross-resource/stale tags, and fresh direct-SQL tests prove invalid or decreasing versions cannot bypass the database boundary.
- Refreshed the implementation ledger to record completed tenant isolation, authorization, API-key lifecycle, runtime health, request correlation, and resource-version groundwork. The remaining priority order now separates the deployment-evidence-gated financial contract, externally configured browser identity, conditional-write adoption, API scale/contracts, continuous controls, and later operations work.
- Added a strict additive pagination contract with bounded limits, canonical opaque cursors bound to one resource and normalized filter set, deterministic ordering tuples, stable failure codes, and exact page envelopes. Contract and HTTP tests cover tied sort keys, malformed/oversized/noncanonical/future cursors, binding mismatches, limit bounds, and header-safe next-page links without changing any legacy endpoint shape.
- Added v1 Rate strong ETags and exact `If-Match` compare-and-swap writes while preserving headerless legacy updates. An expand-only migration makes legacy/direct writes invalidate cached tags and persists ETags for idempotent replay; fresh-database tests cover null-version adoption, tenant-safe 404s, 400/428/412 failures, two stale writers, replay fingerprints, direct writes, and legacy invalidation.
- Replaced delivery-stub console messages that exposed recipient emails, invoice numbers, portal accounts, clearinghouse IDs, and transmission references with a strict Zod-validated operational telemetry shape. Tests prove only approved method/status/artifact/job metadata can serialize, while failed attachment behavior and persisted tenant-visible delivery evidence remain unchanged.
- Added tenant-scoped v1 payment keyset pagination over `(receivedAt DESC, id DESC)` with bounded pages, exact envelopes, and next links while preserving the legacy first-100 array contract. A documented additive index migration and fresh 102-row scenario prove tied timestamps are reachable exactly once, newer interleaved inserts do not shift later pages, malformed/filter-mismatched cursors fail stably, and foreign cursors cannot bypass tenant scope.
- Refreshed the implementation ledger after the first conditional-write and cursor-pagination vertical slices. The remaining API-scale sequence now explicitly extends proven Rate and Payment patterns to financial aggregates and other lists, while documenting SQLite index-build blocking instead of claiming online behavior that the current database cannot provide.
- Added an empty-table, expand-only append-only audit foundation whose actions, actor kinds, and resource kinds come from Prisma-generated enums and whose action/resource pairs are enforced by Zod and database checks. Events retain only tenant, server-derived actor, correlation, hashed idempotency, resource, and time metadata; fresh direct-SQL tests reject foreign tenants, invalid identities/pairs/hashes, updates, and deletes.
- Added a Zod-4/OpenAPI-3.1 operation primitive that makes one definition own Express mounting, input/output contracts, roles, headers, errors, and documentation metadata. All Rate routes now use full version-relative paths from that single source; deterministic tests verify exact inventory, bearer/correlation/idempotency/ETag contracts, duplicate rejection, and redacted failure when a handler violates its response schema.
- Made the still-unreleased audit vocabulary expand-safe on SQLite by moving enum and action/resource validation from immutable table checks to a replaceable insert guard trigger. Added the known combo-discount, invoice-update, and accounting-close actions before controller adoption; future reviewed actions can now ship without rebuilding or rewriting the append-only evidence table.
- Moved Prisma's deprecated `package.json` seed configuration into a typed `prisma.config.ts` while leaving `DATABASE_URL` as an ordinary external environment value. A disposable database applied all production migrations and ran the configured seed successfully without embedding a path, secret, or unsafe fallback in source.
- Made Rate and combo-discount mutations append server-derived actor, request, hashed-idempotency, and resource evidence in the same transaction as each business write. Fresh-database tests prove failed validation, foreign/stale writes, and idempotent replays add no events, while audit-storage failures roll back the associated mutation.
- Made payment receipts, allocations, and application reversals append tenant-bound request evidence atomically with their ledger writes, with one event per created application. Focused unit and fresh-database HTTP tests prove serialization retries and idempotent replays emit exactly once, failures emit nothing, and audit-storage errors roll back every affected balance and ledger row.
- Refreshed `plan.md` to record the append-only audit foundation, Rate/OpenAPI operation contract, Rate/discount/payment audit adoption, generated lifecycle enums, and typed Prisma configuration. The remaining queue now distinguishes extending proven patterns from their already-complete foundations and keeps partial OpenAPI output unpublished until route coverage is complete.
- Routed Order response compatibility numbers through the exact decimal boundary so values must round-trip at the declared quantity or money scale instead of being silently coerced with JavaScript `Number`. Removed the four architecture-rule exceptions, making any raw financial coercion in the Order mapper fail the integrity gate.
- Converted Customer and Product list routes to the shared Zod/OpenAPI operation primitive, so mounted validation, role enforcement, output validation, and generated OpenAPI 3.1 metadata come from the same definitions. Seeded HTTP tests preserve legacy and v1 paths while proving tenant isolation, production authentication, strict query rejection, and successor-link compatibility; the still-partial spec remains unpublished.
- Removed 11 stale Invoice/Payment line exemptions from the raw financial-coercion architecture rule after those mappers moved to the reviewed exact decimal boundary. This prevents unrelated future code landing on an old exempted line from silently bypassing `ARCH002`; only the two current, explicitly reviewed conversions remain.
- Made Order and Invoice lifecycle mutations append authenticated, tenant-bound request evidence inside the same transactions as their business changes, including paired `INVOICE_CREATED`/`ORDER_INVOICED` events and delivery lifecycle resources. Fresh-database tests cover exact actor/hash/resource values, replay and tenant failures, concurrent idempotent creation, no-op refreshes, and rollback of business writes plus earlier audit inserts when any audit append fails.
- Bounded the shared Zod email contract to 254 characters while retaining strict mailbox syntax for customer and invoice-delivery data. Contract tests now reject leading/trailing whitespace, CRLF header injection, malformed structure, and oversized addresses while accepting tagged delivery addresses.
- Converted all Payment routes to shared Zod/OpenAPI operation definitions while preserving the legacy list array and v1 cursor-page behavior, role checks, idempotency, and transactional audit metadata. The operation primitive now supports a versioned documentation schema distinct from a multi-adapter runtime schema, so OpenAPI exposes only the v1 page shape; focused document, route, pagination, and payment-audit tests pass.
- Added v1 Order strong ETags and exact `If-Match` compare-and-swap updates while preserving headerless legacy writes. An expand-only trigger migration invalidates aggregate tags for root, item, comment, and invoice identity/status changes; fresh-database tests cover legacy-null adoption, malformed/cross-resource/stale tags, two writers, replayed ETags with one audit event, and direct/indirect invalidation.
- Added an expand-only Customer email insert/update guard for obvious undeliverable structure without scanning or rewriting existing rows. A copied-history migration rehearsal proves invalid legacy data does not block deployment or unrelated maintenance, while new/changed whitespace, CRLF, malformed-`@`, non-dotted, oversized-local-part, and oversized addresses are rejected at the database boundary.
- Replaced unbounded revenue-report invoice loading with tenant/date/status-filtered, deterministic 250-row Invoice-id keyset pages that derive accounting buckets in SQL and reduce decimal strings through the exact BigInt boundary. Unit and disposable-database tests force multi-page traversal and cover cursor advancement, exact `0.1 + 0.2`, tied dates and customer names, reversals, legacy date fallback, empty periods, and cross-tenant exclusion.
- Converted all Order routes to the shared Zod/OpenAPI operation definitions while preserving legacy writes and the v1 strong-ETag/`If-Match` contract. The same six descriptors now drive Express mounting, role and input checks, strict output validation, and deterministic OpenAPI 3.1 metadata; focused route, document, HTTP, conditional-write, invoice-creation, and tenant-isolation tests pass.
- Converted all three revenue-report routes to the shared Zod/OpenAPI operation definitions, preserving legacy/v1 representations while making strict period input, tenant-read authorization, exact-decimal output, errors, and documentation come from the mounted descriptors. Focused and fresh-database HTTP tests prove invalid-period rejection, cross-tenant exclusion, request correlation, and exact response contracts; the still-partial specification remains unpublished.
- Added v1 Invoice strong ETags and exact `If-Match` compare-and-swap updates while preserving tenant-safe 404s and headerless legacy writes. A trigger-only expand migration invalidates Invoice tags for root, line, payment-application/reversal, and transmission changes; copied-history tests cover null-version adoption, malformed/cross-resource/stale tags, competing writers, exact idempotent replay/audit behavior, and direct evidence writes.
- Added tenant-bound v1 cursor pages for Customer and Product lists while preserving the legacy array contracts and rejecting pagination query parameters on legacy paths. Deterministic `(name, id)` and `(sku, id)` keysets, bounded limits, opaque scoped cursors, next links, and measured low-write-window indexes are covered by fresh-database tests for tied keys, interleaved inserts, malformed/resource/filter/tenant cursor failures, migration inventory, and tenant isolation.
- Refreshed `plan.md` to record completed Order/Invoice conditional writes, catalog pagination, bounded exact reports, email guards, expanded audit coverage, and the Customer/Product/Rate/Order/Payment/Report OpenAPI inventory. The remaining P1 queue now calls out Order/Invoice list pagination, the final Invoice route conversion, tenant-scoped uniqueness contract migrations, authenticated operator audit boundaries, and database-specific exact aggregation without overstating SQLite capabilities.
- Added a conservative SQL migration ratchet to `bun run check` that rejects destructive table/column/index removal, rename/rebuild patterns, top-level backfills or scans, unsupported statements, and malformed trigger/comment/quote boundaries with stable file/line diagnostics. Lexer-driven tests prove comments and strings cannot hide SQL, row-scoped trigger DML remains permitted, bypass attempts fail, and the complete current migration history passes; lock cost and trigger semantics remain explicit deployment-review responsibilities.
- Converted every Invoice, delivery, and transmission route to shared Zod/OpenAPI operation definitions while preserving legacy behavior and v1 strong-ETag/`If-Match` writes. Seven full-path descriptors now own mounting, authorization, strict input/output contracts, idempotency/request headers, exact-decimal schemas, and deterministic OpenAPI 3.1 metadata; focused runtime plus conditional-write, tenant, audit, and delivery regressions pass.
- Added tenant-bound v1 Order cursor pages over deterministic `(orderDate DESC, id DESC)` keysets while preserving the legacy array and rejecting legacy pagination parameters. Controller and HTTP tests prove tied-date traversal, interleaved inserts, strict limits, next links, malformed/resource/filter/tenant cursor failures, tenant isolation, and compatibility with conditional writes; the additive index migration is inventoried and documented as a blocking SQLite build requiring a measured low-write window.
- Validated every current browser API success payload against the shared Zod response contracts before exposing it to UI code, with deterministic failures that do not echo response bodies. Mocked-fetch tests cover exact-decimal success, malformed success JSON, validated Problem Details, non-JSON errors, and empty responses; client typecheck and production build pass.
- Added tenant-bound v1 Invoice cursor pages over deterministic `(issueDate DESC, id DESC)` keysets while preserving the legacy array contract and rejecting pagination parameters there. Controller and live-HTTP tests cover tied dates, interleaved inserts, bounded limits, next links, malformed/resource/filter/tenant cursor failures, exact invoice data, and tenant isolation; the additive index migration is inventoried and documented with its SQLite writer-blocking constraint.
- Made revenue-by-customer labels come from the invoice's immutable customer-name snapshot so later customer renames cannot rewrite historical reporting identity. Unit and disposable-database tests prove captured names remain stable after a rename while null legacy snapshots use the explicitly limited live-customer fallback until backfill completes.
- Split browser-safe pagination schemas from Node-only cursor hashing and exposed conditional contracts-package entrypoints, preserving existing server imports without pulling `node:crypto` or `Buffer` into browser model validation. Module-graph regression coverage, contract tests, client typecheck, and the Vite production build pass with the prior externalization warning eliminated.
- Published the complete v1 OpenAPI 3.1 contract and generated immutable TypeScript client types from the exact operation inventory Express mounts. Deterministic generation plus a non-mutating drift check now run in `bun run check`; focused tests compare the checked-in document, compile a representative generated Invoice contract, and prove runtime output validation remains enforced.
- Replaced production lifecycle strings with values derived from shared Zod enums or Prisma's generated idempotency-state enum, keeping persistence and API vocabulary tied to their declared schemas. New `ARCH006` enforcement rejects reintroduced raw status/method literals outside explicit schema-definition boundaries; 68 focused domain, controller, reconciliation, idempotency, integration, and guard tests pass.
- Added a read-only tenant-contract preflight command that blocks future nullability/table-rebuild work on missing ownership, cross-tenant relationships, target-key duplicates, foreign-key violations, or SQLite integrity errors. Zod-validated reports cap counts and samples deterministically, and disposable migrated/seeded database tests prove clean readiness, blocker detection, existing write guards, and that the audit never repairs or deletes source rows.
- Added tenant-scoped ADMIN API-key issue and revoke endpoints that revalidate the acting credential and append audit evidence in the same transaction. Live migrated-database tests cover role and principal-kind denial, tenant isolation, collision retry, stale or spoofed actors, audit rollback, concurrent and idempotent revocation, final-admin lockout guards, and absence of the one-time token from every persistence path; issuance rejects replay headers and returns non-cacheable.
- Refreshed the execution ledger after completing Order/Invoice pagination, full OpenAPI publication, browser response validation, lifecycle schema derivation, tenant-contract preflight, and audited API-key administration. The remaining queue now separates live-data contract gates, authenticated accounting controls, HTTP/SDK evolution, continuous operations, and database-specific future work.
- Bounded JSON requests at 102,400 bytes with a redacted, non-logged 413 contract, and published actual request-correlation, authentication-challenge, and pagination response headers across OpenAPI. Boundary tests cover exact-limit acceptance, limit-plus-one rejection, first/final page links, auth failures, and idempotent replay; persistence failures now clear stale representation headers, remain non-cacheable, emit redacted evidence, and cannot be blocked by a failing log sink.
- Added an authenticated, tenant-scoped accounting-close endpoint that revalidates the ADMIN API key and couples each monotonic transition to one audit event. Fresh-database HTTP, race, rollback, idempotency, and direct-SQL tests cover missing-date gates, true no-ops, lower-date conflicts, forged/stale actors, control immutability, tenant backfills, finalized invoices, reversals, and the expanded closed-period triggers.
